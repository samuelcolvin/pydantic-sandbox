on: push

jobs:
  experiment:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: python -m pip install httpie

      - run: |
          echo $TOKEN | wc
          http https://api.github.com/repos/samuelcolvin/pydantic-sandbox/releases/latest "Authorization:bearer $TOKEN"
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - run: |
          echo $TOKEN | wc
          http https://api.github.com/repos/samuelcolvin/pydantic/releases/latest "Authorization:bearer $TOKEN"
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
